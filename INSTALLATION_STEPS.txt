═══════════════════════════════════════════════════════════════════
  TASK MANAGEMENT SYSTEM - INSTALLATION STEPS
  Algonive Web Development Internship - Task 2
═══════════════════════════════════════════════════════════════════

📋 QUICK START (Windows)
═══════════════════════════════════════════════════════════════════

METHOD 1: Automated Installation (Recommended)
───────────────────────────────────────────────────────────────────
1. Double-click "install.bat"
2. Wait for dependencies to install
3. Edit backend/.env with your MongoDB URL
4. Double-click "start-dev.bat"
5. Open http://localhost:5173 in your browser

METHOD 2: Manual Installation
───────────────────────────────────────────────────────────────────

STEP 1: Install Backend
───────────────────────────────────────────────────────────────────
cd backend
npm install

STEP 2: Configure Backend Environment
───────────────────────────────────────────────────────────────────
Create backend/.env file with:

PORT=5000
MONGODB_URL=YOUR_MONGODB-URL
JWT_SECRET=your_secret_key_here
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_gmail_app_password
FRONTEND_URL=http://localhost:5173

STEP 3: Start Backend Server
───────────────────────────────────────────────────────────────────
npm start

Backend will run on: http://localhost:5000

STEP 4: Install Frontend (New Terminal)
───────────────────────────────────────────────────────────────────
cd frontend
npm install

STEP 5: Configure Frontend Environment (Optional)
───────────────────────────────────────────────────────────────────
Create frontend/.env file with:

VITE_API_URL=http://localhost:5000/api

STEP 6: Start Frontend Server
───────────────────────────────────────────────────────────────────
npm run dev

Frontend will run on: http://localhost:5173

═══════════════════════════════════════════════════════════════════
  MONGODB ATLAS SETUP
═══════════════════════════════════════════════════════════════════

1. Go to: https://www.mongodb.com/cloud/atlas
2. Sign up for FREE account
3. Create a FREE cluster (M0 Sandbox)
4. Click "Database Access" → Add Database User
5. Click "Network Access" → Add IP Address → Allow Access from Anywhere (0.0.0.0/0)
6. Click "Database" → "Connect" → "Connect your application"
7. Copy connection string
8. Replace <password> with your database password
9. Paste into MONGODB_URL in backend/.env

═══════════════════════════════════════════════════════════════════
  GMAIL SETUP (Optional - For Email Reminders)
═══════════════════════════════════════════════════════════════════

1. Enable 2-Factor Authentication on Gmail
2. Go to: https://myaccount.google.com/security
3. Click "2-Step Verification"
4. Scroll to "App passwords"
5. Select "Mail" and "Windows Computer"
6. Click "Generate"
7. Copy the 16-character password
8. Add to backend/.env:
   EMAIL_USER=youremail@gmail.com
   EMAIL_PASS=abcd efgh ijkl mnop (without spaces)

Note: Email is optional. App works without it!

═══════════════════════════════════════════════════════════════════
  VERIFICATION CHECKLIST
═══════════════════════════════════════════════════════════════════

✓ Backend Dependencies Installed (npm install in backend/)
✓ Frontend Dependencies Installed (npm install in frontend/)
✓ backend/.env file created with MongoDB URL
✓ Backend running on http://localhost:5000
✓ Frontend running on http://localhost:5173
✓ MongoDB Atlas cluster created and connected
✓ Can access login page at http://localhost:5173

═══════════════════════════════════════════════════════════════════
  FIRST TIME USAGE
═══════════════════════════════════════════════════════════════════

1. Open http://localhost:5173
2. Click "Create Account"
3. Fill in:
   - Name: Your Name
   - Email: your@email.com
   - Password: password123 (minimum 6 characters)
   - Role: Admin or Member
4. Click "Create Account"
5. You'll be redirected to Dashboard
6. Click "Create Team" to create your first team
7. Click "Add Member" to invite team members (they need to signup first)
8. Click "Create New Task" to add your first task
9. Explore the Kanban board, notifications, and other features!

═══════════════════════════════════════════════════════════════════
  TROUBLESHOOTING
═══════════════════════════════════════════════════════════════════

Problem: "Port 5000 already in use"
Solution: Change PORT in backend/.env to 5001 or kill the process

Problem: "Cannot connect to MongoDB"
Solution: 
  - Check MongoDB URL is correct
  - Verify IP whitelist (0.0.0.0/0 for testing)
  - Check username and password

Problem: "Frontend can't connect to backend"
Solution:
  - Ensure backend is running on port 5000
  - Check VITE_API_URL in frontend/.env
  - Verify CORS settings in backend/server.js

Problem: "npm install fails"
Solution:
  - Make sure Node.js is installed (v18+)
  - Try: npm cache clean --force
  - Delete node_modules and package-lock.json, then npm install again

Problem: "Email notifications not working"
Solution:
  - This is optional! App works without it
  - Check Gmail credentials
  - Verify app password (not regular password)
  - Enable "Less secure app access" or use App Password

═══════════════════════════════════════════════════════════════════
  USEFUL COMMANDS
═══════════════════════════════════════════════════════════════════

Backend:
  npm start          - Start production server
  npm run dev        - Start with nodemon (auto-reload)

Frontend:
  npm run dev        - Start development server
  npm run build      - Build for production
  npm run preview    - Preview production build

═══════════════════════════════════════════════════════════════════
  PROJECT STRUCTURE
═══════════════════════════════════════════════════════════════════

Algonive - task management/
├── backend/                    # Node.js + Express backend
│   ├── models/                # Database models
│   ├── routes/                # API endpoints
│   ├── utils/                 # Utilities (auth, email, cron)
│   ├── server.js              # Main server file
│   └── package.json
│
├── frontend/                   # React + Vite frontend
│   ├── src/
│   │   ├── components/        # Reusable components
│   │   ├── pages/             # Page components
│   │   ├── context/           # React context
│   │   ├── services/          # API services
│   │   └── App.jsx            # Main app
│   └── package.json
│
├── README.md                   # Full documentation
├── SETUP_GUIDE.md             # Quick setup guide
├── PROJECT_SUMMARY.md         # Project overview
├── install.bat                # Installation script
└── start-dev.bat              # Start both servers

═══════════════════════════════════════════════════════════════════
  FEATURES OVERVIEW
═══════════════════════════════════════════════════════════════════

✓ User Authentication (Signup/Login)
✓ Team Management (Create, Add Members)
✓ Task Management (Create, Edit, Delete)
✓ Task Assignment to Team Members
✓ Status Tracking (Pending → In Progress → Completed)
✓ Priority Levels (Low, Medium, High)
✓ Due Dates with Overdue Indicators
✓ Notifications System
✓ Email Reminders (Optional)
✓ Dashboard with Statistics
✓ Kanban Board View
✓ Search & Filter Tasks
✓ Responsive Design (Mobile, Tablet, Desktop)
✓ Professional Modern UI

═══════════════════════════════════════════════════════════════════
  SUPPORT & DOCUMENTATION
═══════════════════════════════════════════════════════════════════

For detailed information, see:
  - README.md - Complete documentation
  - SETUP_GUIDE.md - Quick setup instructions
  - PROJECT_SUMMARY.md - Project overview and features

═══════════════════════════════════════════════════════════════════

🎉 Happy Task Managing!

═══════════════════════════════════════════════════════════════════
